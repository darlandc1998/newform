<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newform">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_fitness"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.LoginActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.HomeActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.AlunosActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.AlunoActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.ModalidadesActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.ModalidadeActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.PlanosActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.PlanoActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.MatriculasActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.FaturasActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

        <activity
            android:name=".activities.FaturaActivity"
            android:configChanges="orientation"
            android:screenOrientation="fullSensor"/>

    </application>

</manifest>